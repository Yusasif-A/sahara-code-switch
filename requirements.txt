# Voice agent
livekit-agents[openai,silero,noise-cancellation]~=1.0
# >=1.2 for the WhatsApp connector service. 1.0.2 has no `connector` attribute
# at all, which is why the earlier hand-rolled Twirp calls were needed — and why
# they silently failed on guessed field names.
livekit-api>=1.2.1

# HTTP surface for Meta webhooks and the fraud trigger
fastapi~=0.115
uvicorn[standard]~=0.32
pydantic~=2.9

# Support
python-dotenv~=1.0
aiohttp~=3.10

# Tests
pytest~=8.3
pytest-asyncio~=0.24
